

export const glicemyChartFormatter = (glicemyData) =>{

    let data = {
        labels:[],
        dataset:[]
    }

    console.log("a");
   /* glicemyData.forEach( elem =>{
        data.labels.push(elem.time.hours);
        data.dataset.push(elem.value);
    })*/

    return data;
}
